html
    head
        title= title
    body
        h1= message
        form(action='/delete' method="post")
            div(hidden)
                label(for=createdDate)
                input(id= "createdDate", name= "createdDate", value= user.createdDate)
            div(hidden)
                label(for=_id)
                input(id= "_id", name= "_id", value= user._id)
            div
                label(for=firstName) First Name:
                input(id= "firstName", name= "firstName", required= true, value= user.firstName, contenteditable=false)
            div
                label(for=lastName) Last Name:
                input(id= "lastName", name= "lastName", required= true, value= user.lastName, contenteditable=false)
            div
                label(for=email) Email
                input(id= "email", name= "email", required= true, value= user.email, contenteditable=false)
            div
                label(for=password) Password
                input(id= "password", name= "password", required= true, value= user.password, contenteditable=false)
            div
                label(for=role) Role
                input(id= "role", name= "role", required= true, value= user.role, contenteditable=false)
            div
                label(for=age) Age
                input(id= "age", name= "age", required= true, value= user.age, contenteditable=false)
            div
                input(type= "submit")